def repeating_decmials(x,y):
    if not (x, int) or not (y, int):
def repeating_decmials(numerator,denominator):
    if not (numerator, int) or not (denominator, int):
        raise ValueError("num and den must be integers")
    if y == 0:
    if denominator == 0:
        raise ValueError("den cannot be zero")
    result = ""
x=int(input("enter a numerator:"))
y=int(input("enter a denominator:"))
res=x/y
print(res)
    remainder = numerator % denominator
    quotient = numerator // denominator
    result += str(quotient)
    if remainder == 0:
        return result
    result += "."
    remainders = {}
    if remainder == 0:
        return result
    result += "."
    remainders = {}
try:
    numerator = int(input("enter a numerator:"))
    denominator = int(input("enter a denominator:"))
    res = numerator / denominator
    print(res)
